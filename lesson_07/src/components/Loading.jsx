import loading from "../assets/icon/tube-spinner.svg"
function Loading() {
    return (
        <div className="loadeng">
            <img src={loading} alt="Loading" />
        </div>
    );
}

export default Loading;